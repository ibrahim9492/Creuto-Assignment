/**
 * @function generateId
 * @description Generates a unique ID using current timestamp in milliseconds
 * @returns {number} Timestamp in milliseconds
 */

export const generateId = () => +new Date();

